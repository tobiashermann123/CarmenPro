# ── Layer 1: Foresight ───────────────────────────────────────────────────────
unstructured[all-docs]>=0.12.0
llama-index-core>=0.10.57
llama-index-embeddings-huggingface>=0.2.3
sentence-transformers>=3.0.0

# ── Layer 2: Data pipelines (Phase 1C) ───────────────────────────────────────
openbb>=4.2.0
prefect>=2.19.0
gspread>=6.0.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
tavily-python>=0.3.0
schedule>=1.2.0

# ── Layer 3: Storage ─────────────────────────────────────────────────────────
supabase>=2.3.0
python-dotenv>=1.0.0

# ── Layer 4: Valuation (Phase 2) ─────────────────────────────────────────────
# qlib requires a separate install: pip install pyqlib

# ── Layer 5: Execution (Phase 3) ─────────────────────────────────────────────
ib_insync>=0.9.86

# ── Dashboard ────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.22.0

# ── Shared ───────────────────────────────────────────────────────────────────
httpx>=0.27.0
openai>=1.30.0   # vllm OpenAI-compatible API client
